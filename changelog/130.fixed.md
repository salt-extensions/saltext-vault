Fixed handling of `retry_after_max` with urllib3 2.6.3, which introduced the same parameter and set its default to 6h. When `retry_after_max` is explicitly set to `None`, we default to 6h from now on too, otherwise the previous default of 60s applies.
